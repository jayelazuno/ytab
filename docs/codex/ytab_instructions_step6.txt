
Read the completed Step 1, Step 1.5, Step 1.6, Step 2, Step 3, Step 4, and Step 5 implementations for context. Now implement Step 6 only: local MidLC normalization explorer / sample normalization sweep. Do not implement CombineHitFiles, SummaryTable on normalized hits, SummaryTable on combined hits, classifier, or treated-vs-parent analysis yet.

Current successful state:
- Project initialization works.
- Environment setup works.
- glabrata reference is complete and runnable.
- MapFastq local runner works for all 8 small FASTQ samples.
- CreateHitFile local runner works for all 8 samples.
- SummaryTable local runner works.
- Raw LibraryDiagnostics local runner works.
- Raw diagnostics were generated without normalization.
- Hit files are present under:
    output/projects/local_glabrata_smoke_v1/create_hit_file/<sample>/<sample>_hits.txt
- Raw diagnostics are present under:
    output/projects/local_glabrata_smoke_v1/library_diagnostics/
- library_diagnostics.summary.csv is present.

Goal of Step 6:
Create a local, app-compatible normalization explorer that performs MidLC-based normalization using existing LibraryDiagnostics.py normalization functionality. This step should support both:
1. Auto target recommendation based on insertion-site retention.
2. User-requested manual target values, including integers and decimals.

This step should generate normalized hit files and comparison summaries for the Shiny app.

Important:
- Public app is Shiny, not Streamlit.
- Python remains the computational backend.
- Shiny will call CLI wrappers using system2() or processx.
- Do not use qsub.
- Do not use GridEngine/SGE.
- Do not hard-code /Users/jayelazuno, /old_Users, /Volumes, /mnt, or machine-specific paths.
- All paths must come from project.yaml, sample_sheet.csv, reference_resolved.json, CLI args, or repo-relative defaults.
- Do not modify scientific normalization logic unless required to remove hard-coded paths, make inputs/outputs configurable, or allow positive numeric targets.
- Do not run CombineHitFiles yet.
- Do not run SummaryTable on normalized hits yet.
- Do not run SummaryTable on combined parent hits yet.
- Do not run classifier.
- Do not run treated-vs-parent analysis.
- Do not delete old outputs.
- Make the runner restartable and cache-aware.
- Keep memory low.
- Use selected samples from the sample sheet.
- If hit files are missing, fail clearly.
- If raw LibraryDiagnostics has not been run, warn clearly but still allow normalization if required input hit files exist.

Scientific/background logic:
- This step corresponds to the old ytab_SampleNormlization workflow.
- Keep the legacy concept, but implement it as local app-compatible code.
- Use LibraryDiagnostics.py normalization arguments if already supported:
    --normalize-to-midlc <TARGET>
    --normalized-hits-suffix "_normalized_hits.txt"
- Do not reimplement MidLC normalization if LibraryDiagnostics.py already performs it.
- Do not treat fixed targets 20,30,40,50,60,80,100 as the recommended default.
- Fixed target sets are useful for exploration only.
- Default behavior should be adaptive:
    --targets auto
- Auto mode should recommend a conservative target that preserves insertion sites.
- Manual mode should accept any positive numeric target, including integers and decimals.

Default sample logic:
- Default sample set should be parent samples only:
    condition == parent
    or guessed_condition == parent
    or sample name contains "parent"
- Allow user to override and include treated/all samples with CLI options.
- Parent-only default is correct for the later Gale-style essentiality classifier.
- Treated samples are mainly for later fitness comparison and can be normalized optionally, but should not be the default classifier input.

Important retention logic:
- In Step 6, use insertion-site retention as the main metric because feature-level retention requires SummaryTable on normalized hit files, which is a later step.
- For each normalized hit file, compare normalized insertion-site count to raw insertion-site count.
- Compute:
    raw_hit_sites
    normalized_hit_sites
    hit_site_retention_fraction
    hit_site_loss_fraction
    raw_total_reads
    normalized_total_reads
- Default retention threshold:
    hit_site_retention_fraction >= 0.95
- Allow user override:
    --min-site-retention 0.95
- Add this warning to manifests and documentation:
    "Feature-level retention requires SummaryTable on normalized hits and will be confirmed in a later step."

Target behavior:
- Default CLI behavior:
    --targets auto
- Manual targets should support:
    --targets 20
    --targets 20.5
    --targets 59.7
    --targets 20,30,40,50,60,80,100
    --targets 20.5,59.7,100
- Accept any positive numeric target.
- Accept integers and decimals.
- Reject:
    0
    negative values
    NaN
    Inf
    non-numeric values
    empty target list
- Do not restrict targets to the old fixed list.

Target tag formatting:
- Integer targets:
    20 -> T020
    60 -> T060
    100 -> T100
- Decimal targets:
    20.5 -> T020p5
    59.7 -> T059p7
    100.25 -> T100p25
- Do not allow decimal points in folder names.
- Avoid the old octal bug. Treat target tags as strings, never as octal-like numbers.
- Preserve user-provided precision where reasonable, but normalize trailing zeros:
    20.0 -> T020
    20.50 -> T020p5

Auto target recommendation:
- Auto mode should:
    1. Identify selected samples, parent-only by default.
    2. Read raw LibraryDiagnostics outputs if available.
    3. Estimate feasible target range from observed MidLC values if available.
    4. Generate candidate targets from conservative to aggressive.
    5. Run or simulate candidate normalizations as needed to assess insertion-site retention.
    6. Prefer the highest feasible target that satisfies the retention threshold across selected samples.
    7. If no target satisfies threshold, recommend the least aggressive target tested and emit a warning.
- Default auto scan:
    --auto-step 5
- Add CLI options:
    --auto-min-target optional
    --auto-max-target optional
    --auto-step optional, default 5
    --min-site-retention optional, default 0.95
- If observed MidLC values are available:
    auto_min_target should be low but meaningful, for example max(1, floor(min_midlc * 0.25)) unless user provides --auto-min-target.
    auto_max_target should not exceed a conservative estimate of the selected samples' capacity, for example floor(min_midlc) unless user provides --auto-max-target.
- If observed MidLC values are unavailable:
    fall back to a conservative candidate scan and warn clearly:
      "MidLC estimates were not available; using fallback candidate targets."
- Fallback candidate targets may include:
    20,30,40,50,60,80,100
  but this is only a fallback exploration set, not a fixed default recommendation.

Auto recommendation outputs:
- Write:
    output/projects/<PROJECT_ID>/sample_normalization/normalization_recommendation.json
    output/exports/<PROJECT_ID>/qc/sample_normalization/normalization_recommendation.csv
- Include:
    project_id
    species
    selected_samples
    sample_mode
    observed_midlc_values if available
    candidate_targets
    candidate_target_tags
    recommended_target
    recommended_target_tag
    retention_threshold
    min_site_retention_observed
    mean_site_retention_observed
    reason
    warnings
    timestamp
- normalization_recommendation.csv should include:
    recommended_target
    recommended_target_tag
    min_site_retention_observed
    mean_site_retention_observed
    retention_threshold
    reason

Manual target behavior:
- Manual targets should run exactly the requested target values.
- Do not silently replace the user's manual target with the auto target.
- For decimal targets, pass the numeric value to LibraryDiagnostics.py.
- If LibraryDiagnostics.py currently parses --normalize-to-midlc as int, update it to accept positive float while preserving existing integer behavior.
- If the internal normalization mathematically requires integer values at some final step, round only at the final required place and record that in warnings.
- Do not reject decimal user input unless the algorithm truly cannot support it.

Expected local output layout:
  output/projects/<PROJECT_ID>/sample_normalization/<TARGET_TAG>/

Examples:
  output/projects/<PROJECT_ID>/sample_normalization/T020/
  output/projects/<PROJECT_ID>/sample_normalization/T020p5/
  output/projects/<PROJECT_ID>/sample_normalization/T059p7/
  output/projects/<PROJECT_ID>/sample_normalization/T060/
  output/projects/<PROJECT_ID>/sample_normalization/T100/

For each target and sample:
  output/projects/<PROJECT_ID>/sample_normalization/<TARGET_TAG>/<sample>/
    <sample>_normalized_hits.txt
    normalization outputs from LibraryDiagnostics.py

Logs:
  output/projects/<PROJECT_ID>/logs/sample_normalization/

Manifests:
  output/projects/<PROJECT_ID>/manifests/sample_normalization/

Exports:
  output/exports/<PROJECT_ID>/qc/sample_normalization/

Each target should get a manifest:
  output/projects/<PROJECT_ID>/manifests/sample_normalization/<TARGET_TAG>.sample_normalization_manifest.json

Each target/sample should be represented in:
  output/projects/<PROJECT_ID>/manifests/sample_normalization/sample_normalization_status.csv

Status columns:
  target
  target_tag
  sample
  condition
  status
  input_hits_file
  normalized_hits_file
  output_dir
  log_file
  elapsed_seconds
  message

Also create a normalization comparison table:
  output/projects/<PROJECT_ID>/sample_normalization/normalization_comparison.csv
  output/exports/<PROJECT_ID>/qc/sample_normalization/normalization_comparison.csv

normalization_comparison.csv should include:
  target
  target_tag
  sample
  condition
  raw_hit_sites
  normalized_hit_sites
  hit_site_retention_fraction
  hit_site_loss_fraction
  raw_total_reads
  normalized_total_reads
  raw_percent_features_hit
  normalized_percent_features_hit
  normalized_hits_file
  status
  warning

If feature-level values are not available because SummaryTable has not yet been run on normalized hits, leave those values NA and include:
  "Normalized feature-level metrics require SummaryTable on normalized hits in a later step."

Implement:

1. src/ytab/pipeline/normalization_runner.py

Create a local runner module.

Functions to implement:
  load_project_for_normalization(project_config: Path) -> dict
  get_normalization_samples(config: dict, sample_mode: str = "parents", samples: list[str] | None = None) -> list[dict]
  validate_normalization_inputs(config: dict, selected_samples: list[dict]) -> list[str]
  parse_targets(targets: str | list[str] | None) -> str | list[float]
  normalize_target_tag(target: int | float | str) -> str
  find_sample_hits_file(sample: dict, config: dict) -> Path
  count_hit_sites_and_reads(hits_file: Path) -> dict
  build_normalization_command(config: dict, target: float, selected_samples: list[dict], threads: int | None = None) -> list[str]
  run_normalization_target(config: dict, target: float, selected_samples: list[dict], threads: int | None = None, force: bool = False, dry_run: bool = False) -> dict
  recommend_normalization_target(config: dict, selected_samples: list[dict], min_site_retention: float = 0.95, auto_min_target: float | None = None, auto_max_target: float | None = None, auto_step: float = 5.0, threads: int | None = None, force: bool = False, dry_run: bool = False) -> dict
  run_normalization_project(project_config: Path, targets: str | list[str] | None = "auto", sample_mode: str = "parents", samples: list[str] | None = None, threads: int | None = None, force: bool = False, dry_run: bool = False, keep_going: bool = False, min_site_retention: float = 0.95, auto_min_target: float | None = None, auto_max_target: float | None = None, auto_step: float = 5.0) -> dict
  collect_normalization_comparison(config: dict) -> Path | None

Behavior:
- Read project.yaml.
- Read sample_sheet.csv if project.yaml references it.
- Read reference_resolved.json.
- Use hit files from:
    output/projects/<PROJECT_ID>/create_hit_file/<sample>/<sample>_hits.txt
- Determine selected samples:
    sample_mode="parents" default
    sample_mode="all" for all included samples
    sample_mode="treated" for treated samples
    samples list overrides mode
- For manual targets:
    run normalization for requested target values.
- For auto:
    run candidate targets, calculate insertion-site retention, recommend target, and ensure recommended target normalized outputs are available.
- Prefer one LibraryDiagnostics call per target across selected hit files if the CLI supports multiple hit files.
- Write outputs under:
    output/projects/<PROJECT_ID>/sample_normalization/<TARGET_TAG>/
- Preserve per-sample subdirectories if LibraryDiagnostics.py creates them.
- If LibraryDiagnostics.py writes all outputs flat, reorganize/copy normalized hit files into per-sample target folders.
- Write logs under:
    output/projects/<PROJECT_ID>/logs/sample_normalization/
- Write manifests under:
    output/projects/<PROJECT_ID>/manifests/sample_normalization/
- Copy useful QC outputs to:
    output/exports/<PROJECT_ID>/qc/sample_normalization/
- Skip a target/sample if successful manifest and expected normalized hit file already exist unless force=True.
- dry_run=True should print/write commands but not execute.
- keep_going=True should continue remaining targets if one target fails.
- keep_going=False should stop at first failed target.
- Return a summary dictionary.

Important:
- build_normalization_command must inspect and use the current LibraryDiagnostics.py CLI.
- Do not invent CLI flags if LibraryDiagnostics.py already defines different names.
- Do not use normalization flags in Step 5; Step 6 is the first normalization step.

2. Patch src/ytab/qc/LibraryDiagnostics.py only if needed

Requirements:
- Preserve raw diagnostics behavior from Step 5.
- Ensure normalization can write normalized hit files to a chosen output directory.
- Ensure normalization accepts multiple input hit files.
- Ensure --normalize-to-midlc and --normalized-hits-suffix work if they already exist.
- Ensure --normalize-to-midlc accepts positive numeric values, including decimals, if the underlying algorithm can support them.
- Ensure command-line help works:
    python src/ytab/qc/LibraryDiagnostics.py --help
- Do not change MidLC scientific logic.

3. scripts/local/ytab_run_sample_normalization.py

Create a CLI wrapper.

Usage:
  python scripts/local/ytab_run_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets auto \
    --sample-mode parents \
    --min-site-retention 0.95 \
    --threads 2

Options:
  --project-config required
  --targets optional, default auto
      accepts:
        auto
        comma-separated positive numeric values
  --sample-mode optional, choices parents, all, treated, default parents
  --samples optional comma-separated sample names
  --threads optional
  --force optional
  --dry-run optional
  --keep-going optional
  --min-site-retention optional, default 0.95
  --auto-min-target optional positive numeric
  --auto-max-target optional positive numeric
  --auto-step optional positive numeric, default 5

Behavior:
- Print project id.
- Print species.
- Print selected sample mode.
- Print selected samples.
- Print target mode:
    auto or manual
- If manual:
    print targets and target tags.
- If auto:
    print auto scan settings.
- Print input hit files.
- Print output normalization directory.
- In dry-run mode, print commands and exit 0 without creating normalized hits.
- In normal mode, run normalization target by target or auto scan.
- Write per-target manifests and combined status CSV.
- Write normalization_comparison.csv.
- In auto mode, write normalization_recommendation.json and normalization_recommendation.csv.
- Exit nonzero if any selected target/sample fails, unless dry-run.
- If keep-going is enabled, continue but still exit nonzero at end if any failed.

4. scripts/local/ytab_smoke_sample_normalization.py

Create a lightweight smoke runner.

Usage:
  python scripts/local/ytab_smoke_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets auto \
    --sample-mode parents

Also support manual smoke:
  python scripts/local/ytab_smoke_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets 60 \
    --sample-mode parents

Behavior:
- Defaults to auto target recommendation on parent samples.
- Prints selected samples.
- Prints input hit files.
- Prints target mode.
- Prints output directory.
- Prints detected normalized hit files.
- Prints recommendation file if auto mode is used.
- Prints manifest path.
- Prints PASS/FAIL.
- Does not run SummaryTable on normalized hits.
- Does not combine hit files.
- Does not run classifier.

5. Update app/shiny/app.R

Add a “Normalization Explorer” placeholder section after Library Diagnostics.

It should allow:
- project_config path input
- mode selector:
    Auto recommend target
    Manual targets
- Auto mode options:
    min site retention, default 0.95
    auto min target optional
    auto max target optional
    auto step, default 5
- Manual target field:
    comma-separated positive numeric values
    examples: 20, 20.5, 59.7, 100
- sample mode selector:
    parents
    all
    treated
- dry-run checkbox
- force checkbox
- button:
    "Run normalization"

The button should call:
  python scripts/local/ytab_run_sample_normalization.py --project-config <project.yaml> --targets auto --sample-mode <mode> --min-site-retention <value>

or, for manual mode:
  python scripts/local/ytab_run_sample_normalization.py --project-config <project.yaml> --targets <manual_targets> --sample-mode <mode>

Add optional:
  --dry-run
  --force

Display command output/log text in the app.

If normalization_recommendation.csv exists, display it in a basic table using DT if available.
If normalization_comparison.csv exists, display it in a basic table using DT if available.

Add this note:
  "Auto mode recommends a conservative target based on insertion-site retention. Feature-level retention will be confirmed after SummaryTable is run on normalized hits."

Do not implement final target selection for classifier yet, but add a placeholder note:
  "Final classifier target selection will be added after normalized SummaryTable and combined parent hits are implemented."

6. Update docs/setup_local.md

Add a Step 6 section:

  Explore MidLC normalization

Include commands:
  mamba activate ytab-local

Auto dry-run:
  python scripts/local/ytab_run_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets auto \
    --sample-mode parents \
    --min-site-retention 0.95 \
    --threads 2 \
    --dry-run

Auto smoke:
  python scripts/local/ytab_smoke_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets auto \
    --sample-mode parents

Manual decimal smoke:
  python scripts/local/ytab_smoke_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets 59.7 \
    --sample-mode parents

Manual exploratory sweep:
  python scripts/local/ytab_run_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets 20,30,40,50,60,80,100 \
    --sample-mode parents \
    --threads 2 \
    --keep-going

Explain:
- Normalization runs on parent samples by default.
- Parent-normalized hit files are used later for the Gale-style classifier workflow.
- Default mode is auto target recommendation, not a fixed target list.
- Auto mode recommends a conservative target based on insertion-site retention.
- Feature-level retention will be checked later after SummaryTable is run on normalized hits.
- Users can manually request any positive numeric target, including decimal values.
- Existing successful targets are skipped unless --force is used.
- Input hit files come from output/projects/<PROJECT_ID>/create_hit_file/.
- Output normalized hit files are written to output/projects/<PROJECT_ID>/sample_normalization/.
- This step does not run SummaryTable on normalized hits yet.
- This step does not combine parent pools yet.
- This step does not run classifier yet.

7. Checks

Run:
  python -m py_compile src/ytab/pipeline/normalization_runner.py
  python -m py_compile scripts/local/ytab_run_sample_normalization.py
  python -m py_compile scripts/local/ytab_smoke_sample_normalization.py
  python src/ytab/qc/LibraryDiagnostics.py --help

Dry-run auto:
  python scripts/local/ytab_run_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets auto \
    --sample-mode parents \
    --min-site-retention 0.95 \
    --threads 2 \
    --dry-run

Smoke auto:
  python scripts/local/ytab_smoke_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets auto \
    --sample-mode parents

Smoke manual decimal:
  python scripts/local/ytab_smoke_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets 59.7 \
    --sample-mode parents

If smoke passes, optionally run the exploratory parent sweep:
  python scripts/local/ytab_run_sample_normalization.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets 20,30,40,50,60,80,100 \
    --sample-mode parents \
    --threads 2 \
    --keep-going

Expected after successful auto normalization:
  output/projects/local_glabrata_smoke_v1/sample_normalization/
  output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_recommendation.json
  output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_comparison.csv
  output/projects/local_glabrata_smoke_v1/manifests/sample_normalization/sample_normalization_status.csv
  output/exports/local_glabrata_smoke_v1/qc/sample_normalization/

Expected after successful manual decimal normalization:
  output/projects/local_glabrata_smoke_v1/sample_normalization/T059p7/

8. Hard-coded path cleanup

Search for hard-coded paths in new/modified local files:
  grep -R "/Users/jayelazuno\\|/old_Users\\|/Volumes/rdss\\|/mnt/archived_data\\|/workspace/HermesTnseq" -n src scripts app docs

Rules:
- Do not introduce hard-coded paths.
- Existing legacy HPC scripts may contain old paths; leave them alone.
- New local/app files must not contain hard-coded local or HPC paths.
- LibraryDiagnostics.py should not require hard-coded paths for normalization.

9. App organization compatibility

Ensure new local runner is compatible with this app flow:
  Shiny app
    -> project.yaml
    -> sample_sheet.csv
    -> reference_resolved.json
    -> ytab_run_sample_normalization.py
    -> output/projects/<PROJECT_ID>/sample_normalization/
    -> manifests/status files
    -> normalization_recommendation.json/csv
    -> normalization_comparison.csv
    -> Shiny reads status/results

Do not require users to manually edit Python code.

10. Stop point

Stop after Step 6.
Do not implement:
- CombineHitFiles
- SummaryTable on normalized hits
- SummaryTable on combined parent hits
- classifier
- treated-vs-parent screen

After implementation, show:
  git status --short
  find src/ytab/pipeline scripts/local app/shiny output/projects/local_glabrata_smoke_v1 -maxdepth 4 -type f | sort

Summarize:
- files created/modified
- whether auto dry-run passed
- whether auto parent-only normalization smoke test passed
- whether manual decimal normalization smoke test passed
- whether exploratory parent target sweep passed, if run
- number of normalized hit files produced
- recommended target from auto mode
- where outputs were written
- any failures or missing resources
