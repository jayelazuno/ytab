
Read the completed Step 1 through Step 6 implementations for context. Now implement Step 7 only: SummaryTable on normalized hit files and normalization target evaluation. Do not implement CombineHitFiles, SummaryTable on combined parent hits, classifier, or treated-vs-parent analysis yet.

Current successful state:
- Project initialization works.
- Environment setup works.
- glabrata reference is complete and runnable.
- MapFastq local runner works for all 8 small FASTQ samples.
- CreateHitFile local runner works for all 8 samples.
- Raw SummaryTable runner works.
- Raw LibraryDiagnostics runner works.
- Step 6 normalization runner works.
- Step 6 produced normalized hit files under:
    output/projects/local_glabrata_smoke_v1/sample_normalization/<TARGET_TAG>/<sample>/<sample>_normalized_hits.txt
- Step 6 produced:
    output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_comparison.csv
    output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_recommendation.json
- Feature-level normalized metrics are currently NA, as expected.

Goal of Step 7:
Run SummaryTable on normalized hit files, usually parent samples only, so the app can evaluate normalization targets using feature-level metrics before combining parent pools.

Important:
- Public app is Shiny, not Streamlit.
- Python remains the computational backend.
- Shiny calls CLI wrappers using system2() or processx.
- Do not use qsub.
- Do not use GridEngine/SGE.
- Do not hard-code /Users/jayelazuno, /old_Users, /Volumes, /mnt, or machine-specific paths.
- All paths must come from project.yaml, sample_sheet.csv, reference_resolved.json, CLI args, or repo-relative defaults.
- Do not modify scientific SummaryTable logic unless required for configurable inputs/outputs.
- Do not combine parent pools yet.
- Do not run classifier.
- Do not run treated-vs-parent analysis.
- Do not delete old outputs.
- Make the runner restartable and cache-aware.
- Keep memory low.
- Process one normalized sample at a time by default.

Default sample logic:
- Default sample mode should be parents.
- Use samples where:
    condition == parent
    or guessed_condition == parent
    or sample name contains parent
- Allow sample-mode all and treated as overrides.
- Allow explicit --samples comma-separated override.

Target logic:
- User can provide:
    --targets auto
    --targets recommended
    --targets all
    --targets T060
    --targets 60
    --targets 59.7
    --targets T059p7
    --targets 20,30,40,50,60,80,100
- If --targets recommended:
    read normalization_recommendation.json and use recommended_target_tag.
- If --targets auto:
    treat as recommended if normalization_recommendation.json exists.
    If recommendation is missing, fail clearly and tell user to run Step 6 first.
- If --targets all:
    detect all target directories under output/projects/<PROJECT_ID>/sample_normalization/T*/
- Manual targets should resolve numeric values to target tags using the same target-tag logic from Step 6.
- Do not rerun normalization in Step 7.
- Step 7 only summarizes normalized hits that already exist.

Expected local output layout:
  output/projects/<PROJECT_ID>/summary_normalized/<TARGET_TAG>/<sample>/
  output/projects/<PROJECT_ID>/logs/summary_normalized/
  output/projects/<PROJECT_ID>/manifests/summary_normalized/
  output/exports/<PROJECT_ID>/qc/summary_normalized/<TARGET_TAG>/

Each normalized sample should produce normal SummaryTable outputs, including feature-level tables such as:
  *.feature_table*.csv
  *.feature_table*.tsv
  *.feature_table*.txt

Do not invent exact SummaryTable output names if SummaryTable.py already produces specific names. Detect and record actual outputs.

Each normalized target/sample should get a manifest:
  output/projects/<PROJECT_ID>/manifests/summary_normalized/<TARGET_TAG>/<sample>.summary_normalized_manifest.json

Also write/update:
  output/projects/<PROJECT_ID>/manifests/summary_normalized/summary_normalized_status.csv

Status columns:
  target
  target_tag
  sample
  condition
  status
  input_normalized_hits_file
  output_dir
  feature_tables
  log_file
  elapsed_seconds
  message

Also create a target evaluation table:
  output/projects/<PROJECT_ID>/sample_normalization/normalization_target_evaluation.csv
  output/exports/<PROJECT_ID>/qc/sample_normalization/normalization_target_evaluation.csv

normalization_target_evaluation.csv should include:
  target
  target_tag
  sample
  condition
  raw_hit_sites
  normalized_hit_sites
  hit_site_retention_fraction
  raw_total_reads
  normalized_total_reads
  raw_percent_features_hit
  normalized_percent_features_hit
  feature_retention_fraction
  feature_loss_fraction
  raw_total_hits
  normalized_total_hits
  raw_mean_hits_per_feature
  normalized_mean_hits_per_feature
  normalized_hits_file
  normalized_feature_table
  status
  warning

If raw SummaryTable stats are available from:
  output/projects/<PROJECT_ID>/summary/summary_stats.all_samples.csv
or per-sample SummaryTable outputs, use them.

If raw stats are unavailable, leave raw feature fields NA and warn:
  "Raw feature-level metrics unavailable; run raw SummaryTable first."

If normalized SummaryTable succeeds, fill normalized feature-level metrics.

Also create a target-level summary:
  output/projects/<PROJECT_ID>/sample_normalization/normalization_target_summary.csv
  output/exports/<PROJECT_ID>/qc/sample_normalization/normalization_target_summary.csv

normalization_target_summary.csv should include:
  target
  target_tag
  sample_count
  min_hit_site_retention_fraction
  mean_hit_site_retention_fraction
  min_feature_retention_fraction
  mean_feature_retention_fraction
  max_feature_loss_fraction
  mean_normalized_percent_features_hit
  status
  recommended_by_site_retention
  recommended_by_feature_retention

Recommendation refinement:
- Do not overwrite the Step 6 recommendation.
- Add a new file:
    output/projects/<PROJECT_ID>/sample_normalization/normalization_feature_recommendation.json
    output/exports/<PROJECT_ID>/qc/sample_normalization/normalization_feature_recommendation.csv
- This should refine target recommendation using feature-level retention.
- Default minimum feature retention:
    --min-feature-retention 0.95
- A target is feature-safe if:
    feature_retention_fraction >= min_feature_retention across selected samples
- If multiple targets pass, recommend the highest target that passes.
- If none pass, recommend the least aggressive target tested and warn.
- For the 10k smoke subset, note that recommendations are workflow tests, not biological recommendations.

Implement:

1. src/ytab/pipeline/summary_normalized_runner.py

Create a local runner module.

Functions to implement:
  load_project_for_summary_normalized(project_config: Path) -> dict
  get_summary_normalized_samples(config: dict, sample_mode: str = "parents", samples: list[str] | None = None) -> list[dict]
  resolve_normalization_targets(config: dict, targets: str | list[str] = "recommended") -> list[dict]
  find_normalized_hits_file(config: dict, target_tag: str, sample: dict) -> Path
  validate_summary_normalized_inputs(config: dict, target_infos: list[dict], selected_samples: list[dict]) -> list[str]
  build_summary_normalized_command(config: dict, target_info: dict, sample: dict, threads: int | None = None) -> list[str]
  run_summary_normalized_sample(config: dict, target_info: dict, sample: dict, threads: int | None = None, force: bool = False, dry_run: bool = False) -> dict
  run_summary_normalized_project(project_config: Path, targets: str | list[str] = "recommended", sample_mode: str = "parents", samples: list[str] | None = None, threads: int | None = None, force: bool = False, dry_run: bool = False, keep_going: bool = False, min_feature_retention: float = 0.95) -> dict
  collect_normalization_target_evaluation(config: dict, min_feature_retention: float = 0.95) -> dict

Behavior:
- Read project.yaml.
- Read sample_sheet.csv if project.yaml references it.
- Read reference_resolved.json.
- Use normalized hit files from:
    output/projects/<PROJECT_ID>/sample_normalization/<TARGET_TAG>/<sample>/<sample>_normalized_hits.txt
- Use reference annotation paths from reference_resolved.json.
- Validate normalized hit files exist.
- Validate feature_table or GFF/GTF exists.
- Only run selected samples.
- Support target filtering.
- Write outputs under:
    output/projects/<PROJECT_ID>/summary_normalized/<TARGET_TAG>/<sample>/
- Write logs under:
    output/projects/<PROJECT_ID>/logs/summary_normalized/
- Write manifests under:
    output/projects/<PROJECT_ID>/manifests/summary_normalized/
- Copy or collect useful QC tables to:
    output/exports/<PROJECT_ID>/qc/summary_normalized/<TARGET_TAG>/
- Skip a target/sample if successful manifest and expected feature table already exist unless force=True.
- dry_run=True should print/write commands but not execute.
- keep_going=True should continue remaining target/sample jobs if one fails.
- keep_going=False should stop at first failed target/sample.
- Return a summary dictionary.

Important:
- build_summary_normalized_command must inspect and use current SummaryTable.py CLI.
- Do not invent CLI flags if SummaryTable.py already defines different names.
- Do not change SummaryTable scientific logic.

2. Patch src/ytab/summary/SummaryTable.py only if needed

Requirements:
- Preserve raw SummaryTable behavior from Step 4.
- Ensure it can accept normalized hit files as input.
- Ensure output directory is configurable.
- Ensure sample prefix/name is configurable.
- Ensure command-line help works:
    python src/ytab/summary/SummaryTable.py --help
- Do not change scientific logic.

3. scripts/local/ytab_run_summary_normalized.py

Create a CLI wrapper.

Usage:
  python scripts/local/ytab_run_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets recommended \
    --sample-mode parents \
    --threads 2

Options:
  --project-config required
  --targets optional, default recommended
      accepts recommended, auto, all, target tags, numeric targets, comma-separated lists
  --sample-mode optional, choices parents, all, treated, default parents
  --samples optional comma-separated sample names
  --threads optional
  --force optional
  --dry-run optional
  --keep-going optional
  --min-feature-retention optional, default 0.95

Behavior:
- Print project id.
- Print species.
- Print selected sample mode.
- Print selected samples.
- Print selected targets and target tags.
- Print input normalized hit files.
- Print output normalized summary directory.
- In dry-run mode, print commands and exit 0 without creating normalized summary tables.
- In normal mode, run SummaryTable for each selected target/sample.
- Write per-target/sample manifests and combined status CSV.
- Write normalization_target_evaluation.csv.
- Write normalization_target_summary.csv.
- Write normalization_feature_recommendation.json/csv.
- Exit nonzero if any selected target/sample fails, unless dry-run.
- If keep-going is enabled, continue but still exit nonzero at end if any failed.

4. scripts/local/ytab_smoke_summary_normalized.py

Create a lightweight smoke runner.

Usage:
  python scripts/local/ytab_smoke_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets recommended \
    --sample-mode parents

Also support:
  python scripts/local/ytab_smoke_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets T059p7 \
    --sample-mode parents

Behavior:
- Runs normalized SummaryTable for selected target on parent samples.
- Defaults to recommended target.
- Prints selected target(s).
- Prints selected samples.
- Prints input normalized hit files.
- Prints output directory.
- Prints detected normalized feature tables.
- Prints target evaluation file.
- Prints feature recommendation file.
- Prints PASS/FAIL.
- Does not combine hit files.
- Does not run classifier.

5. Update app/shiny/app.R

Add a “Normalized Summary / Target Evaluation” placeholder section after Normalization Explorer.

It should allow:
- project_config path input
- target selector:
    recommended
    all
    manual
- manual target field:
    accepts comma-separated tags or positive numeric values
- sample mode selector:
    parents
    all
    treated
- min feature retention input, default 0.95
- dry-run checkbox
- force checkbox
- button:
    "Run normalized SummaryTable"

The button should call:
  python scripts/local/ytab_run_summary_normalized.py --project-config <project.yaml> --targets <targets> --sample-mode <mode> --min-feature-retention <value>

Add optional:
  --dry-run
  --force

Display command output/log text in the app.

If normalization_target_evaluation.csv exists, display it using DT if available.
If normalization_target_summary.csv exists, display it using DT if available.
If normalization_feature_recommendation.csv exists, display it using DT if available.

Add note:
  "This step evaluates feature-level retention after normalization. Parent-pool combining and classifier input selection come next."

6. Update docs/setup_local.md

Add a Step 7 section:

  Evaluate normalized hit files with SummaryTable

Include commands:
  mamba activate ytab-local

Dry-run:
  python scripts/local/ytab_run_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets recommended \
    --sample-mode parents \
    --threads 2 \
    --dry-run

Smoke:
  python scripts/local/ytab_smoke_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets recommended \
    --sample-mode parents

Manual target:
  python scripts/local/ytab_smoke_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets T059p7 \
    --sample-mode parents

All targets:
  python scripts/local/ytab_run_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets all \
    --sample-mode parents \
    --threads 2 \
    --keep-going

Explain:
- This step runs SummaryTable on normalized hit files.
- It fills feature-level retention metrics.
- It helps choose a safe normalization target.
- It does not combine parent pools yet.
- It does not run classifier yet.

7. Checks

Run:
  python -m py_compile src/ytab/pipeline/summary_normalized_runner.py
  python -m py_compile scripts/local/ytab_run_summary_normalized.py
  python -m py_compile scripts/local/ytab_smoke_summary_normalized.py
  python src/ytab/summary/SummaryTable.py --help

Dry-run:
  python scripts/local/ytab_run_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets recommended \
    --sample-mode parents \
    --threads 2 \
    --dry-run

Smoke:
  python scripts/local/ytab_smoke_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets recommended \
    --sample-mode parents

Manual target smoke:
  python scripts/local/ytab_smoke_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets T059p7 \
    --sample-mode parents

If smoke passes, optionally run all normalized targets:
  python scripts/local/ytab_run_summary_normalized.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --targets all \
    --sample-mode parents \
    --threads 2 \
    --keep-going

Expected outputs:
  output/projects/local_glabrata_smoke_v1/summary_normalized/
  output/projects/local_glabrata_smoke_v1/manifests/summary_normalized/summary_normalized_status.csv
  output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_target_evaluation.csv
  output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_target_summary.csv
  output/projects/local_glabrata_smoke_v1/sample_normalization/normalization_feature_recommendation.json
  output/exports/local_glabrata_smoke_v1/qc/summary_normalized/
  output/exports/local_glabrata_smoke_v1/qc/sample_normalization/normalization_target_evaluation.csv

8. Hard-coded path cleanup

Search for hard-coded paths in new/modified local files:
  grep -R "/Users/jayelazuno\\|/old_Users\\|/Volumes/rdss\\|/mnt/archived_data\\|/workspace/HermesTnseq" -n src scripts app docs

Rules:
- Do not introduce hard-coded paths.
- Existing legacy HPC scripts may contain old paths; leave them alone.
- New local/app files must not contain hard-coded local or HPC paths.
- SummaryTable.py should not require hard-coded paths for normalized operation.

9. App organization compatibility

Ensure new local runner is compatible with this app flow:
  Shiny app
    -> project.yaml
    -> sample_sheet.csv
    -> reference_resolved.json
    -> ytab_run_summary_normalized.py
    -> output/projects/<PROJECT_ID>/summary_normalized/
    -> manifests/status files
    -> normalization_target_evaluation.csv
    -> normalization_feature_recommendation.json/csv
    -> Shiny reads status/results

Do not require users to manually edit Python code.

10. Stop point

Stop after Step 7.
Do not implement:
- CombineHitFiles
- SummaryTable on combined parent hits
- classifier
- treated-vs-parent screen

After implementation, show:
  git status --short
  find src/ytab/pipeline scripts/local app/shiny output/projects/local_glabrata_smoke_v1 -maxdepth 4 -type f | sort

Summarize:
- files created/modified
- whether dry-run passed
- whether recommended-target normalized SummaryTable smoke test passed
- whether manual T059p7 smoke test passed
- whether all-target run passed, if run
- feature-level retention summary
- recommended target after feature-level evaluation
- where outputs were written
- any failures or missing resources
