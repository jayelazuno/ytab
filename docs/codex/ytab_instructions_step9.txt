
Read the completed Step 1 through Step 8 implementations for context. Now implement Step 9 only: SummaryTable on the combined normalized parent hit file. Do not implement classifier or treated-vs-parent analysis yet.

Current successful state:
- MapFastq works.
- CreateHitFile works.
- Raw SummaryTable works.
- Raw LibraryDiagnostics works.
- Normalization explorer works.
- SummaryTable on normalized individual parent hits works.
- Combine normalized parent hits works.
- Step 8 produced combined parent hit files:
    output/projects/local_glabrata_smoke_v1/combined_hits/T6400/combined_parent_hits.T6400.txt
    output/projects/local_glabrata_smoke_v1/combined_hits/T059p7/combined_parent_hits.T059p7.txt
- Recommended target for the smoke project is:
    T6400
- Note: T6400 is not biologically meaningful for the 10k-read smoke subset; it is acceptable for workflow testing.

Goal of Step 9:
Create a local, app-compatible runner that runs SummaryTable on one combined normalized parent hit file for a selected normalization target. This produces the combined parent feature table that will be used later as classifier input.

Important:
- Public app is Shiny, not Streamlit.
- Python remains the computational backend.
- Shiny calls CLI wrappers using system2() or processx.
- Do not use qsub.
- Do not use GridEngine/SGE.
- Do not hard-code /Users/jayelazuno, /old_Users, /Volumes, /mnt, or machine-specific paths.
- All paths must come from project.yaml, sample_sheet.csv, reference_resolved.json, CLI args, or repo-relative defaults.
- Do not modify scientific SummaryTable logic unless required for configurable input/output paths.
- Do not run classifier.
- Do not run treated-vs-parent analysis.
- Do not delete old outputs.
- Make the runner restartable and cache-aware.
- Keep memory low.

Scientific logic:
- Use existing src/ytab/summary/SummaryTable.py.
- Input is a combined normalized parent hit file generated by Step 8.
- Output is a combined parent feature table.
- This is the correct Gale-style order:
    normalized parent hit files
    -> combine normalized parent hit files
    -> SummaryTable on combined normalized parent library
    -> classifier later
- Do not run SummaryTable separately on individual normalized hits in this step. That was Step 7.
- Do not combine hits in this step. That was Step 8.

Target logic:
- User can provide:
    --target recommended
    --target auto
    --target feature-recommended
    --target site-recommended
    --target T6400
    --target T059p7
    --target 59.7
    --target 60
- If --target recommended or --target feature-recommended:
    read output/projects/<PROJECT_ID>/sample_normalization/normalization_feature_recommendation.json
    use recommended_target_tag
- If feature recommendation is missing:
    fall back to output/projects/<PROJECT_ID>/sample_normalization/normalization_recommendation.json
    and warn clearly
- If --target auto:
    treat as recommended
- If --target site-recommended:
    read normalization_recommendation.json
- If manual numeric target is provided:
    convert to target tag using the same tag logic from Step 6
- If manual target tag is provided:
    use it directly after validating that the combined hits directory exists
- Do not rerun normalization.
- Do not rerun combine hits.

Expected local output layout:
  output/projects/<PROJECT_ID>/summary_combined/<TARGET_TAG>/
  output/projects/<PROJECT_ID>/logs/summary_combined/
  output/projects/<PROJECT_ID>/manifests/summary_combined/
  output/exports/<PROJECT_ID>/qc/summary_combined/<TARGET_TAG>/

Input:
  output/projects/<PROJECT_ID>/combined_hits/<TARGET_TAG>/combined_parent_hits.<TARGET_TAG>.txt

Expected main output:
  output/projects/<PROJECT_ID>/summary_combined/<TARGET_TAG>/combined_feature_table.<TARGET_TAG>.txt

Important:
- SummaryTable.py may generate a feature table with its own name.
- Detect the actual feature table produced by SummaryTable.
- If exactly one main feature table is detected, copy or symlink it to:
    combined_feature_table.<TARGET_TAG>.txt
- This stable filename will be used later by the classifier.
- Do not alter table contents during this stable-copy step.
- If multiple candidate feature tables are found, choose the main feature-level table using existing SummaryTable naming conventions and record the decision in the manifest.
- If no feature table is found, fail clearly.

Also create:
  output/projects/<PROJECT_ID>/summary_combined/<TARGET_TAG>/combined_summary_stats.<TARGET_TAG>.csv

If SummaryTable.py already produces stats, copy/collect them.
If not, create a minimal stats table from the detected feature table if safe.
Do not invent biological metrics if unavailable.

Manifest:
  output/projects/<PROJECT_ID>/manifests/summary_combined/<TARGET_TAG>.summary_combined_manifest.json

Manifest should include:
  project_id
  species
  target
  target_tag
  input_combined_hits_file
  output_dir
  detected_feature_tables
  stable_combined_feature_table
  summary_stats_file
  log_file
  status: success/failed/skipped
  start_time
  end_time
  elapsed_seconds
  command_run
  warnings
  error_message if failed

Status CSV:
  output/projects/<PROJECT_ID>/manifests/summary_combined/summary_combined_status.csv

Status columns:
  target
  target_tag
  status
  input_combined_hits_file
  stable_combined_feature_table
  summary_stats_file
  output_dir
  log_file
  elapsed_seconds
  message

Implement:

1. src/ytab/pipeline/summary_combined_runner.py

Create a local runner module.

Functions to implement:
  load_project_for_summary_combined(project_config: Path) -> dict
  resolve_summary_combined_target(config: dict, target: str = "recommended") -> dict
  find_combined_hits_file(config: dict, target_tag: str) -> Path
  validate_summary_combined_inputs(config: dict, target_info: dict) -> list[str]
  build_summary_combined_command(config: dict, target_info: dict, threads: int | None = None) -> list[str]
  detect_combined_feature_tables(output_dir: Path) -> list[Path]
  choose_main_combined_feature_table(feature_tables: list[Path]) -> Path
  run_summary_combined_project(project_config: Path, target: str = "recommended", threads: int | None = None, force: bool = False, dry_run: bool = False) -> dict
  collect_combined_summary_stats(config: dict, target_info: dict, feature_table: Path) -> Path | None

Behavior:
- Read project.yaml.
- Read sample_sheet.csv if needed.
- Read reference_resolved.json.
- Resolve target tag.
- Find combined hit file from:
    output/projects/<PROJECT_ID>/combined_hits/<TARGET_TAG>/combined_parent_hits.<TARGET_TAG>.txt
- Validate combined hit file exists.
- Validate feature_table or GFF/GTF exists from reference_resolved.json.
- Run SummaryTable.py once for the combined parent hit file.
- Use sample/prefix name:
    combined_parent
  or:
    combined_parent_<TARGET_TAG>
- Write outputs under:
    output/projects/<PROJECT_ID>/summary_combined/<TARGET_TAG>/
- Write logs under:
    output/projects/<PROJECT_ID>/logs/summary_combined/
- Write manifests under:
    output/projects/<PROJECT_ID>/manifests/summary_combined/
- Copy useful outputs to:
    output/exports/<PROJECT_ID>/qc/summary_combined/<TARGET_TAG>/
- Create stable combined feature table:
    combined_feature_table.<TARGET_TAG>.txt
- Skip if successful manifest and stable combined feature table already exist unless force=True.
- dry_run=True should print/write command but not execute.
- Return a summary dictionary.

Important:
- build_summary_combined_command must inspect and use current SummaryTable.py CLI.
- Do not invent CLI flags if SummaryTable.py already defines different names.
- Do not change SummaryTable scientific logic.

2. Patch src/ytab/summary/SummaryTable.py only if needed

Requirements:
- Preserve raw SummaryTable behavior from Step 4.
- Preserve normalized SummaryTable behavior from Step 7.
- Ensure it can accept a combined hit file as input.
- Ensure output directory is configurable.
- Ensure sample prefix/name is configurable.
- Ensure command-line help works:
    python src/ytab/summary/SummaryTable.py --help
- Do not change scientific logic.

3. scripts/local/ytab_run_summary_combined.py

Create a CLI wrapper.

Usage:
  python scripts/local/ytab_run_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target recommended \
    --threads 2

Options:
  --project-config required
  --target optional, default recommended
      accepts recommended, auto, feature-recommended, site-recommended, target tags, numeric targets
  --threads optional
  --force optional
  --dry-run optional

Behavior:
- Print project id.
- Print species.
- Print selected target and target tag.
- Print input combined hit file.
- Print output summary_combined directory.
- Print expected stable combined feature table path.
- In dry-run mode, print command and exit 0 without running SummaryTable.
- In normal mode, run SummaryTable on the combined parent hit file.
- Write manifest, status CSV, and stable combined feature table.
- Exit nonzero if SummaryTable fails or no feature table is detected.

4. scripts/local/ytab_smoke_summary_combined.py

Create a lightweight smoke runner.

Usage:
  python scripts/local/ytab_smoke_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target recommended

Also support:
  python scripts/local/ytab_smoke_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target T059p7

Behavior:
- Runs SummaryTable on one combined parent hit file.
- Defaults to recommended target.
- Prints selected target.
- Prints input combined hit file.
- Prints output directory.
- Prints detected feature table.
- Prints stable combined feature table.
- Prints manifest path.
- Prints PASS/FAIL.
- Does not run classifier.

5. Update app/shiny/app.R

Add a “Combined Parent SummaryTable” placeholder section after Combine Parent Hits.

It should allow:
- project_config path input
- target selector:
    recommended
    site-recommended
    feature-recommended
    manual
- manual target field:
    accepts target tags or positive numeric values
- dry-run checkbox
- force checkbox
- button:
    "Build combined parent SummaryTable"

The button should call:
  python scripts/local/ytab_run_summary_combined.py --project-config <project.yaml> --target <target>

Add optional:
  --dry-run
  --force

Display command output/log text in the app.

If summary_combined_status.csv exists, display it using DT if available.

Add note:
  "This step creates the combined parent feature table used by the essentiality classifier. Classifier execution comes next."

6. Update docs/setup_local.md

Add a Step 9 section:

  Build SummaryTable for combined parent library

Include commands:
  mamba activate ytab-local

Dry-run:
  python scripts/local/ytab_run_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target recommended \
    --threads 2 \
    --dry-run

Smoke recommended:
  python scripts/local/ytab_smoke_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target recommended

Manual target:
  python scripts/local/ytab_smoke_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target T059p7

Explain:
- This step runs SummaryTable on the combined normalized parent hit file.
- It creates a stable combined feature table:
    output/projects/<PROJECT_ID>/summary_combined/<TARGET_TAG>/combined_feature_table.<TARGET_TAG>.txt
- This stable feature table is the input for the classifier.
- This step does not run the classifier yet.

7. Checks

Run:
  python -m py_compile src/ytab/pipeline/summary_combined_runner.py
  python -m py_compile scripts/local/ytab_run_summary_combined.py
  python -m py_compile scripts/local/ytab_smoke_summary_combined.py
  python src/ytab/summary/SummaryTable.py --help

Dry-run:
  python scripts/local/ytab_run_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target recommended \
    --threads 2 \
    --dry-run

Smoke recommended:
  python scripts/local/ytab_smoke_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target recommended

Smoke manual:
  python scripts/local/ytab_smoke_summary_combined.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --target T059p7

Expected outputs:
  output/projects/local_glabrata_smoke_v1/summary_combined/T6400/
  output/projects/local_glabrata_smoke_v1/summary_combined/T6400/combined_feature_table.T6400.txt
  output/projects/local_glabrata_smoke_v1/manifests/summary_combined/T6400.summary_combined_manifest.json
  output/projects/local_glabrata_smoke_v1/manifests/summary_combined/summary_combined_status.csv
  output/exports/local_glabrata_smoke_v1/qc/summary_combined/T6400/

If manual T059p7 smoke is run:
  output/projects/local_glabrata_smoke_v1/summary_combined/T059p7/combined_feature_table.T059p7.txt

8. Hard-coded path cleanup

Search for hard-coded paths in new/modified local files:
  grep -R "/Users/jayelazuno\\|/old_Users\\|/Volumes/rdss\\|/mnt/archived_data\\|/workspace/HermesTnseq" -n src scripts app docs

Rules:
- Do not introduce hard-coded paths.
- Existing legacy HPC scripts may contain old paths; leave them alone.
- New local/app files must not contain hard-coded local or HPC paths.
- SummaryTable.py should not require hard-coded paths for combined operation.

9. App organization compatibility

Ensure new local runner is compatible with this app flow:
  Shiny app
    -> project.yaml
    -> sample_sheet.csv
    -> reference_resolved.json
    -> combined_hits/<TARGET_TAG>/combined_parent_hits.<TARGET_TAG>.txt
    -> ytab_run_summary_combined.py
    -> output/projects/<PROJECT_ID>/summary_combined/<TARGET_TAG>/
    -> combined_feature_table.<TARGET_TAG>.txt
    -> manifests/status files
    -> Shiny reads status/results

Do not require users to manually edit Python code.

10. Stop point

Stop after Step 9.
Do not implement:
- classifier
- treated-vs-parent screen

After implementation, show:
  git status --short
  find src/ytab/pipeline scripts/local app/shiny output/projects/local_glabrata_smoke_v1 -maxdepth 4 -type f | sort

Summarize:
- files created/modified
- whether dry-run passed
- whether recommended-target combined SummaryTable passed
- whether manual T059p7 combined SummaryTable passed, if run
- stable combined feature table path
- number of features in combined table, if available
- where outputs were written
- any failures or missing resources
