
Read the completed Step 1, Step 1.5, Step 1.6, Step 2, Step 3, and Step 4 implementations for context. Now implement Step 5 only: local LibraryDiagnostics runner. Do not implement normalization explorer, CombineHitFiles, classifier, or treated-vs-parent analysis yet.

Current successful state:
- Project initialization works.
- Environment setup works.
- glabrata reference is complete and runnable.
- MapFastq local runner works for all 8 small FASTQ samples.
- CreateHitFile local runner works for all 8 samples.
- SummaryTable local runner works.
- Hit files are present under:
    output/projects/local_glabrata_smoke_v1/create_hit_file/<sample>/<sample>_hits.txt
- Summary outputs are present under:
    output/projects/local_glabrata_smoke_v1/summary/<sample>/
- BigWig is optional and should not block diagnostics.

Goal of Step 5:
Create a local, app-compatible LibraryDiagnostics runner that reads per-sample hit files and generates raw library QC diagnostics using the existing src/ytab/qc/LibraryDiagnostics.py logic.

Important:
- Public app is Shiny, not Streamlit.
- Python remains the computational backend.
- Shiny will call CLI wrappers using system2() or processx.
- Do not use qsub.
- Do not use GridEngine/SGE.
- Do not hard-code /Users/jayelazuno, /old_Users, /Volumes, /mnt, or machine-specific paths.
- All paths must come from project.yaml, sample_sheet.csv, reference_resolved.json, CLI args, or repo-relative defaults.
- Do not modify scientific diagnostics logic unless required to remove hard-coded paths or make inputs/outputs configurable.
- Do not run normalization yet.
- Do not run classifier.
- Do not run treated-vs-parent analysis.
- Do not delete old outputs.
- Make the runner restartable and cache-aware.
- Keep memory low.
- Use selected samples from the sample sheet.
- If hit files are missing, fail clearly.
- If centromere BED or tRNA BED is missing, warn clearly but do not fail unless LibraryDiagnostics.py requires them.

Important distinction:
- LibraryDiagnostics.py may already support normalization arguments such as --normalize-to-midlc or --normalized-hits-suffix.
- Do not use those in Step 5.
- Step 5 is raw library diagnostics only.
- Normalization explorer will be Step 6.

Expected local output layout:
  output/projects/<PROJECT_ID>/library_diagnostics/
  output/projects/<PROJECT_ID>/logs/library_diagnostics/
  output/projects/<PROJECT_ID>/manifests/library_diagnostics/
  output/exports/<PROJECT_ID>/qc/diagnostics/

Expected useful outputs may include:
  library_diagnostics.summary.csv
  <sample>.summary.csv
  <sample>.midlc.csv
  <sample>.seqbias_2_7.tsv
  <sample>.centromere_bins.tsv
  <sample>.centromere_bias.png
  <sample>.tss_metaplot.tsv
  <sample>.tts_metaplot.tsv
  <sample>.trna_metaplot.tsv
  <sample>.metaplots.png

Do not invent output names if LibraryDiagnostics.py already produces specific names. Detect and record actual outputs.

Each run should get a manifest:
  output/projects/<PROJECT_ID>/manifests/library_diagnostics/library_diagnostics_manifest.json

The manifest should include:
  project_id
  species
  selected_samples
  input_hit_files
  output_dir
  export_dir
  log_file
  status: success/failed/skipped
  start_time
  end_time
  elapsed_seconds
  command_run
  detected_outputs
  warnings
  error_message if failed

Also write/update a status file:
  output/projects/<PROJECT_ID>/manifests/library_diagnostics/library_diagnostics_status.csv

Status columns:
  project_id
  status
  samples
  input_hit_files_count
  output_dir
  export_dir
  log_file
  elapsed_seconds
  message

Implement:

1. src/ytab/pipeline/library_diagnostics_runner.py

Create a local runner module.

Functions to implement:
  load_project_for_library_diagnostics(project_config: Path) -> dict
  get_included_samples(config: dict) -> list[dict]
  validate_library_diagnostics_inputs(config: dict, samples: list[str] | None = None) -> list[str]
  find_sample_hits_file(sample: dict, config: dict) -> Path
  build_library_diagnostics_command(config: dict, samples: list[str] | None = None, threads: int | None = None) -> list[str]
  run_library_diagnostics_project(project_config: Path, samples: list[str] | None = None, threads: int | None = None, force: bool = False, dry_run: bool = False) -> dict

Behavior:
- Read project.yaml.
- Read sample_sheet.csv if project.yaml references it.
- Read reference_resolved.json.
- Use hit files from:
    output/projects/<PROJECT_ID>/create_hit_file/<sample>/<sample>_hits.txt
- Use reference paths from reference_resolved.json when needed:
    FASTA
    feature_table or GFF/GTF
    centromere BED
    tRNA BED
- Only include samples where include is true, unless --samples is specified.
- Support sample filtering by name.
- LibraryDiagnostics should usually run once across the selected hit files, not one independent process per sample, because it produces cross-library QC summaries.
- Build the command by inspecting the current LibraryDiagnostics.py CLI.
- Do not invent CLI flags if LibraryDiagnostics.py already defines different names.
- Known likely pattern:
    python src/ytab/qc/LibraryDiagnostics.py \
      --hits-txt <hit1> <hit2> ... \
      --out-dir <output_dir>
  But inspect --help and adapt to actual options.
- Write outputs under:
    output/projects/<PROJECT_ID>/library_diagnostics/
- Write logs under:
    output/projects/<PROJECT_ID>/logs/library_diagnostics/
- Write manifest/status under:
    output/projects/<PROJECT_ID>/manifests/library_diagnostics/
- Copy or collect useful QC files to:
    output/exports/<PROJECT_ID>/qc/diagnostics/
- Skip if a previous successful manifest exists and expected summary output exists unless force=True.
- dry_run=True should print/write commands but not execute.
- Return a summary dictionary.

2. Patch src/ytab/qc/LibraryDiagnostics.py only if needed

Requirements:
- Remove or avoid hard-coded paths.
- Ensure it accepts:
    multiple input hit files
    output directory
    optional feature annotation/reference files if required
    optional centromere BED
    optional tRNA BED
- Ensure command-line help works:
    python src/ytab/qc/LibraryDiagnostics.py --help
- Preserve existing behavior used by HPC scripts if possible.
- If backward compatibility aliases are needed, keep them.
- Do not change QC scientific logic.
- Do not force normalization in Step 5.

3. scripts/local/ytab_run_library_diagnostics.py

Create a CLI wrapper.

Usage:
  python scripts/local/ytab_run_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml

Options:
  --project-config required
  --samples optional comma-separated sample names
  --threads optional
  --force optional
  --dry-run optional

Behavior:
- Print project id.
- Print species.
- Print number of included samples.
- Print selected samples.
- Print input hit files.
- Print output diagnostics directory.
- In dry-run mode, print command and exit 0 without running diagnostics.
- In normal mode, run LibraryDiagnostics once across selected samples.
- Write manifest and status CSV.
- Copy/collect exports.
- Exit nonzero if diagnostics fails.

4. scripts/local/ytab_smoke_library_diagnostics.py

Create a lightweight smoke runner.

Usage:
  python scripts/local/ytab_smoke_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --samples yH298-parent-pool1,yH298-parent-pool2

Behavior:
- Runs LibraryDiagnostics on a small selected subset, defaulting to first two included samples if --samples is omitted.
- Prints input hit files.
- Prints output directory.
- Prints detected summary output(s).
- Prints manifest path.
- Prints PASS/FAIL.
- Does not run normalization or downstream steps.

5. Update app/shiny/app.R minimally

Add a “Library Diagnostics” placeholder section after Build Summary Tables.

It should allow:
- project_config path input
- sample selection text box or “all included samples”
- dry-run checkbox
- button: “Run LibraryDiagnostics”

The button should call:
  python scripts/local/ytab_run_library_diagnostics.py --project-config <project.yaml>

Add optional --dry-run if selected.

Display command output/log text in the app.

Do not implement normalization explorer or downstream pages.

6. Update docs/setup_local.md

Add a Step 5 section:

  Run library diagnostics

Include commands:
  mamba activate ytab-local

  python scripts/local/ytab_run_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --dry-run

  python scripts/local/ytab_smoke_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --samples yH298-parent-pool1,yH298-parent-pool2

  python scripts/local/ytab_run_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml

Explain:
- LibraryDiagnostics runs across selected hit files.
- Existing successful diagnostics are skipped unless --force is used.
- Input hit files come from output/projects/<PROJECT_ID>/create_hit_file/.
- Output diagnostics are written to output/projects/<PROJECT_ID>/library_diagnostics/.
- Exported QC files are copied to output/exports/<PROJECT_ID>/qc/diagnostics/.
- This step does not normalize hit files yet.

7. Checks

Run:
  python -m py_compile src/ytab/pipeline/library_diagnostics_runner.py
  python -m py_compile scripts/local/ytab_run_library_diagnostics.py
  python -m py_compile scripts/local/ytab_smoke_library_diagnostics.py
  python src/ytab/qc/LibraryDiagnostics.py --help

Dry-run:
  python scripts/local/ytab_run_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --dry-run

If at least two hit files exist, run:
  python scripts/local/ytab_smoke_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --samples yH298-parent-pool1,yH298-parent-pool2

If smoke passes, run full diagnostics:
  python scripts/local/ytab_run_library_diagnostics.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --force

Expected after successful full diagnostics:
  output/projects/local_glabrata_smoke_v1/library_diagnostics/
  output/projects/local_glabrata_smoke_v1/logs/library_diagnostics/
  output/projects/local_glabrata_smoke_v1/manifests/library_diagnostics/library_diagnostics_manifest.json
  output/projects/local_glabrata_smoke_v1/manifests/library_diagnostics/library_diagnostics_status.csv
  output/exports/local_glabrata_smoke_v1/qc/diagnostics/

8. Hard-coded path cleanup

Search for hard-coded paths in new/modified local files:
  grep -R "/Users/jayelazuno\\|/old_Users\\|/Volumes/rdss\\|/mnt/archived_data\\|/workspace/HermesTnseq" -n src scripts app docs

Rules:
- Do not introduce hard-coded paths.
- Existing legacy HPC scripts may contain old paths; leave them alone.
- New local/app files must not contain hard-coded local or HPC paths.
- LibraryDiagnostics.py should not require hard-coded paths for local operation.

9. App organization compatibility

Ensure new local runner is compatible with this app flow:
  Shiny app
    -> project.yaml
    -> sample_sheet.csv
    -> reference_resolved.json
    -> ytab_run_library_diagnostics.py
    -> output/projects/<PROJECT_ID>/library_diagnostics/
    -> manifests/status files
    -> Shiny reads status/results

Do not require users to manually edit Python code.

10. Stop point

Stop after Step 5.
Do not implement:
- normalization explorer
- CombineHitFiles
- SummaryTable on combined parent hits
- classifier
- treated-vs-parent screen

After implementation, show:
  git status --short
  find src/ytab/pipeline scripts/local app/shiny output/projects/local_glabrata_smoke_v1 -maxdepth 4 -type f | sort

Summarize:
- files created/modified
- whether dry-run passed
- whether two-sample LibraryDiagnostics smoke test passed
- whether full diagnostics passed, if run
- where outputs were written
- any failures or missing resources
