
Read the completed Step 1 through Step 10 implementations for context. Now implement Step 11 only: local treated-versus-parent fitness analysis using raw per-sample SummaryTable outputs and CPM normalization inside the existing R analysis script.

Do not implement final report generation, one-click orchestration, deployment, or additional scientific analysis stages.

CURRENT SUCCESSFUL STATE

The following stages work:

- Project initialization
- Local environment setup
- Reference discovery and preparation
- MapFastq
- CreateHitFile
- Raw per-sample SummaryTable
- Raw LibraryDiagnostics
- Parent-only MidLC normalization
- SummaryTable on normalized parent libraries
- Combining normalized parent hit files
- SummaryTable on the combined normalized parent library
- Essentiality classifier

The essentiality-classifier workflow is complete and separate from the treated-versus-parent workflow.

Current smoke samples:

Parents:
  yH298-parent-pool1
  yH298-parent-pool2
  yH299-parent-pool3
  yH299-parent-pool4

H2O2-treated:
  yH298-H2O2-treated-facs-pool1
  yH298-H2O2-treated-facs-pool2
  yH299-H2O2-treated-facs-pool3
  yH299-H2O2-treated-facs-pool4

Expected smoke pairings:

  yH298-parent-pool1
    vs yH298-H2O2-treated-facs-pool1

  yH298-parent-pool2
    vs yH298-H2O2-treated-facs-pool2

  yH299-parent-pool3
    vs yH299-H2O2-treated-facs-pool3

  yH299-parent-pool4
    vs yH299-H2O2-treated-facs-pool4

These sample names are examples for the smoke project. Do not hard-code them into the general workflow.

NON-NEGOTIABLE SCIENTIFIC WORKFLOW

The treated-versus-parent analysis must use raw per-sample feature-level SummaryTable outputs from:

  output/projects/<PROJECT_ID>/summary/<sample>/

The established scientific workflow is:

  raw per-sample SummaryTable feature tables
  -> CPM normalization inside scripts/ytab_treated_vs_parent_screen.R
  -> treated-versus-parent fitness comparison

MidLC normalization is used only for the essentiality-classifier workflow.

Do not use any of the following as treated-versus-parent inputs:

  output/projects/<PROJECT_ID>/sample_normalization/
  output/projects/<PROJECT_ID>/summary_normalized/
  output/projects/<PROJECT_ID>/combined_hits/
  output/projects/<PROJECT_ID>/summary_combined/
  output/projects/<PROJECT_ID>/classifier/
  final_classifier_target.txt
  final_classifier_target.json
  normalization_recommendation.json
  normalization_feature_recommendation.json

Do not automatically reuse:

  T6400
  T059p7
  any MidLC target
  any classifier normalization target
  any combined parent feature table

The Step 11 CLI and Shiny app must not offer MidLC-normalized inputs as an analysis mode.

The only supported scientific input mode for Step 11 is:

  raw-summary

For compatibility, the CLI may accept:

  --input-mode auto
  --input-mode raw-summary

But:

  --input-mode auto

must always resolve to:

  raw-summary

Reject unsupported input modes clearly, including:

  raw-hits
  normalized-hits
  normalized-summary
  combined-hits
  combined-summary
  classifier

If the current R script does not perform CPM normalization internally, stop and report the discrepancy. Do not substitute another normalization method and do not silently move CPM normalization into Python.

GOAL OF STEP 11

Create a local, app-compatible treated-versus-parent analysis runner around:

  scripts/ytab_treated_vs_parent_screen.R

The runner should:

1. Create or validate an explicit parent-treated comparison design.
2. Resolve raw per-sample SummaryTable feature tables.
3. Validate that CPM normalization is implemented in the R script.
4. Run the existing R scientific analysis.
5. Preserve all original R outputs.
6. Create stable result tables and run metadata.
7. Expose the design and analysis results in the Shiny app.
8. Remain restartable, cache-aware, and free of machine-specific paths.

SCIENTIFIC PRESERVATION RULES

Inspect scripts/ytab_treated_vs_parent_screen.R before modifying it.

Preserve the existing:

- CPM normalization
- pseudocount handling
- parent-versus-treated comparison logic
- replicate or pair handling
- fold-change calculation
- statistical tests
- multiple-testing correction
- significance thresholds
- fold-change thresholds
- gene or feature identifier handling
- scientific output columns

Do not change these merely to simplify the wrapper.

Do not fabricate:

- P values
- adjusted P values
- fold changes
- depletion calls
- enrichment calls
- fitness scores
- significance classifications

Only report metrics and classifications actually produced by the existing analysis.

GENERAL IMPLEMENTATION REQUIREMENTS

- The public app is Shiny, not Streamlit.
- Python remains the orchestration backend.
- R remains the scientific treated-versus-parent analysis backend.
- Shiny calls the Python CLI wrapper using system2() or processx.
- The Python wrapper invokes Rscript.
- Discover Rscript using the active environment or PATH.
- Do not hard-code the Rscript executable path.
- Do not use qsub.
- Do not use GridEngine or SGE.
- Do not introduce machine-specific paths.
- Do not hard-code:
    /Users/jayelazuno
    /old_Users
    /Volumes
    /mnt
    workspace/HermesTnseq
- Resolve paths from:
    project.yaml
    sample_sheet.csv
    comparison_design.csv
    reference_resolved.json
    CLI arguments
    repository-relative scripts
    project-relative output directories
- Do not delete previous outputs.
- Make the runner restartable and cache-aware.
- Keep memory usage appropriate for a computer with approximately 8 GB RAM.
- Use hashes for relevant inputs and the R script.
- Do not require users to edit Python or R source code.

STEP 11A: COMPARISON DESIGN

Create:

  output/projects/<PROJECT_ID>/config/comparison_design.csv

Required columns:

  comparison_id
  pair_id
  parent_sample
  treated_sample
  background
  pool
  parent_condition
  treated_condition
  include
  notes

For the smoke project, generate four included pairs corresponding to pools 1 through 4.

Design-generation priority:

1. Explicit sample-sheet metadata
2. Shared background identifiers
3. Shared pool identifiers
4. Treatment and condition labels
5. Sample-name parsing only as a fallback

Parent detection priority:

  condition == parent
  guessed_condition == parent
  treatment == untreated
  sample name contains parent

Treated detection priority:

  condition == treated
  guessed_condition == treated
  treatment is non-empty and not untreated
  sample name contains treated

Pair parents and treated samples using matching:

  background
  pool

Do not pair samples solely by row order.

Do not silently guess ambiguous pairings.

Flag:

- one parent matching multiple treated samples
- one treated sample matching multiple parents
- treated sample without a parent
- parent sample without a treated match
- duplicate comparison IDs
- duplicate pair IDs
- missing background
- missing pool
- sample not present in sample_sheet.csv
- sample excluded in sample_sheet.csv
- parent and treated sample being the same
- parent sample assigned to multiple included pairs when not explicitly allowed
- treated sample assigned to multiple included pairs when not explicitly allowed

Write unresolved issues to:

  output/projects/<PROJECT_ID>/config/comparison_design_issues.csv

Require user review if unresolved or ambiguous included pairings remain.

The comparison design should remain user-editable.

Implement reusable functions:

  load_or_create_comparison_design(...)
  infer_comparison_design(...)
  validate_comparison_design(...)
  write_comparison_design(...)
  get_included_comparisons(...)

STEP 11B: RAW SUMMARY INPUT RESOLUTION

Each included sample should resolve to one raw Step 4 SummaryTable output under:

  output/projects/<PROJECT_ID>/summary/<sample>/

Do not assume the exact SummaryTable feature-table filename.

Use the same robust feature-table detection logic established in Steps 4, 7, and 9.

For each parent and treated sample:

- detect candidate feature-level tables
- select the primary SummaryTable feature table
- ensure it is non-empty
- validate required identifier and count columns expected by the R script
- record the selected file in metadata

If multiple ambiguous feature tables are found, fail clearly.

If no feature table is found, fail with:

  "Raw SummaryTable output is missing for sample <SAMPLE>. Run Step 4 before treated-versus-parent analysis."

Input-mode rules:

Supported:

  --input-mode auto
  --input-mode raw-summary

Behavior:

  auto -> raw-summary

Reject any other value with a clear error:

  "Treated-versus-parent analysis supports raw per-sample SummaryTable inputs only. MidLC-normalized inputs are reserved for the essentiality classifier."

Do not add:

  --normalization-target

to the Step 11 CLI.

Do not expose normalization-target controls in the Step 11 Shiny interface.

STEP 11C: VERIFY CPM NORMALIZATION

Implement inspection of:

  scripts/ytab_treated_vs_parent_screen.R

The inspection should identify evidence that the script performs CPM normalization, such as:

- counts per million calculation
- library-size division multiplied by 1e6
- an established CPM function
- an equivalent existing implementation clearly documented in the script

Do not rely solely on the presence of the literal string "CPM" if the actual calculation can be inspected more directly.

The runner should record:

  cpm_normalization_detected
  cpm_detection_evidence
  normalization_method

If CPM normalization cannot be confirmed:

- stop before running the analysis
- return a clear error
- do not implement an alternative normalization
- do not use MidLC normalization
- do not silently proceed with raw unnormalized values

Required error:

  "CPM normalization could not be confirmed in scripts/ytab_treated_vs_parent_screen.R. Step 11 requires the established raw-summary to CPM workflow. Review the R script before continuing."

STEP 11D: OUTPUT LAYOUT

Project output:

  output/projects/<PROJECT_ID>/treated_vs_parent/<ANALYSIS_ID>/

Logs:

  output/projects/<PROJECT_ID>/logs/treated_vs_parent/

Manifests:

  output/projects/<PROJECT_ID>/manifests/treated_vs_parent/

Exports:

  output/exports/<PROJECT_ID>/treated_vs_parent/<ANALYSIS_ID>/

Default analysis ID:

  treated_vs_parent_raw_cpm

For the H2O2 project, allow:

  H2O2_vs_parent

Sanitize analysis IDs for directory use.

Preserve all files generated by the R script.

Create stable outputs when corresponding results are available:

  treated_vs_parent_results.csv
  treated_vs_parent_comparison_summary.csv
  treated_vs_parent_run_metadata.json

If the R script runs each pair separately, preserve pair outputs under:

  pairs/<PAIR_ID>/

Create a combined stable result table:

  treated_vs_parent_results.csv

The stable result table must retain all scientific columns from the R outputs.

Only add explicit provenance columns when needed:

  comparison_id
  pair_id
  parent_sample
  treated_sample
  background
  pool

Do not rename the R script’s scientific columns.

Potential outputs to preserve when actually generated:

- gene-level fitness tables
- insertion-count tables
- CPM tables
- parent and treated CPM values
- fold-change values
- P values
- adjusted P values
- depletion or enrichment classifications
- volcano plots
- parent-versus-treated scatterplots
- replicate-correlation plots
- QC summaries
- normalization diagnostics

Do not require outputs the existing R analysis does not produce.

STEP 11E: OPTIONAL CLASSIFIER ANNOTATION

Classifier annotation is optional and must remain separate from the fitness calculation.

Do not use classifier predictions to:

- filter genes
- remove result rows
- determine which tests are run
- alter CPM normalization
- alter fold changes
- alter P values
- alter adjusted P values
- alter depletion or enrichment calls

If classifier annotation is requested:

- resolve an existing prediction table
- join only after the treated-versus-parent analysis is complete
- join using a validated feature or gene identifier
- preserve every treated-versus-parent result row
- add classifier columns with clear prefixes if needed
- record matched and unmatched identifiers
- do not infer classifier label meanings without metadata

Add CLI options:

  --annotate-classifier
  --classifier-target <TARGET>

Do not enable annotation by default.

For smoke data, add:

  "Classifier annotations originate from a shallow workflow-validation dataset and should not be interpreted as final biological essentiality calls."

STEP 11F: RUN METADATA

Create:

  output/projects/<PROJECT_ID>/treated_vs_parent/<ANALYSIS_ID>/treated_vs_parent_run_metadata.json

Include:

  project_id
  species
  analysis_id
  input_mode
  comparison_design
  comparison_design_sha256
  included_comparisons
  parent_samples
  treated_samples
  raw_summary_input_files
  input_file_sha256
  R_script
  R_script_sha256
  R_version
  relevant_R_package_versions
  cpm_normalization_detected
  cpm_detection_evidence
  normalization_method
  analysis_method
  pseudocount
  significance_thresholds
  fold_change_thresholds
  multiple_testing_method
  classifier_annotation_used
  classifier_prediction_table
  classifier_annotation_match_count
  classifier_annotation_unmatched_count
  warnings
  timestamp

Only include method and parameter values that are detected from the R script or supplied explicitly.

Do not claim a statistical method merely because it is common.

STEP 11G: MANIFEST AND STATUS

Manifest:

  output/projects/<PROJECT_ID>/manifests/treated_vs_parent/<ANALYSIS_ID>.treated_vs_parent_manifest.json

Include:

  project_id
  species
  analysis_id
  input_mode
  comparison_design_file
  selected_comparisons
  parent_samples
  treated_samples
  input_files
  output_dir
  stable_result_table
  comparison_summary_file
  run_metadata_file
  log_file
  status
  start_time
  end_time
  elapsed_seconds
  command_run
  detected_outputs
  input_hashes
  comparison_design_hash
  script_hash
  analysis_parameters
  warnings
  error_message if failed

Status CSV:

  output/projects/<PROJECT_ID>/manifests/treated_vs_parent/treated_vs_parent_status.csv

Columns:

  analysis_id
  status
  input_mode
  comparison_count
  parent_samples
  treated_samples
  stable_result_table
  output_dir
  log_file
  elapsed_seconds
  message

Caching:

Skip a completed analysis only when all are true:

- manifest status is success
- stable result table exists
- comparison-design hash is unchanged
- raw SummaryTable input hashes are unchanged
- R script hash is unchanged
- analysis parameters are unchanged
- classifier annotation settings are unchanged
- force=False

Rerun when any relevant input, design, script, or parameter changes.

IMPLEMENTATION

1. src/ytab/pipeline/treated_vs_parent_runner.py

Create this module.

Functions:

  load_project_for_treated_vs_parent(
      project_config: Path
  ) -> dict

  infer_comparison_design(
      config: dict,
      overwrite: bool = False
  ) -> Path

  load_comparison_design(
      design_file: Path
  ) -> list[dict]

  validate_comparison_design(
      config: dict,
      design_rows: list[dict]
  ) -> dict

  get_included_comparisons(
      design_rows: list[dict],
      comparisons: list[str] | None = None
  ) -> list[dict]

  detect_raw_summary_feature_table(
      config: dict,
      sample: str
  ) -> Path

  inspect_treated_vs_parent_script(
      script_path: Path
  ) -> dict

  verify_cpm_normalization(
      script_inspection: dict
  ) -> dict

  resolve_treated_vs_parent_inputs(
      config: dict,
      comparisons: list[dict],
      input_mode: str = "auto"
  ) -> dict

  resolve_optional_classifier_annotation(
      config: dict,
      target: str | None = None
  ) -> dict | None

  validate_treated_vs_parent_inputs(
      config: dict,
      comparisons: list[dict],
      resolved_inputs: dict,
      script_inspection: dict
  ) -> list[str]

  build_treated_vs_parent_command(
      config: dict,
      analysis_id: str,
      comparisons: list[dict],
      resolved_inputs: dict,
      output_dir: Path,
      classifier_annotation: dict | None = None,
      extra_args: list[str] | None = None
  ) -> list[str]

  detect_treated_vs_parent_outputs(
      output_dir: Path
  ) -> list[Path]

  choose_primary_treated_vs_parent_table(
      outputs: list[Path]
  ) -> Path

  create_stable_treated_vs_parent_outputs(
      config: dict,
      analysis_id: str,
      comparisons: list[dict],
      outputs: list[Path]
  ) -> dict

  annotate_treated_vs_parent_results(
      result_table: Path,
      classifier_annotation: dict
  ) -> dict

  write_treated_vs_parent_metadata(
      config: dict,
      analysis_id: str,
      comparisons: list[dict],
      resolved_inputs: dict,
      script_inspection: dict,
      outputs: list[Path],
      classifier_annotation: dict | None = None
  ) -> Path

  run_treated_vs_parent_project(
      project_config: Path,
      analysis_id: str = "treated_vs_parent_raw_cpm",
      comparison_design: Path | None = None,
      comparisons: list[str] | None = None,
      input_mode: str = "auto",
      classifier_target: str | None = None,
      annotate_classifier: bool = False,
      force: bool = False,
      dry_run: bool = False,
      keep_going: bool = False,
      extra_args: list[str] | None = None
  ) -> dict

Behavior:

- Read project.yaml.
- Read sample_sheet.csv.
- Create comparison_design.csv when absent.
- Validate included comparisons.
- Resolve raw Step 4 SummaryTable feature tables.
- Inspect the R script.
- Confirm CPM normalization.
- Refuse to run when CPM cannot be confirmed.
- Run the R analysis.
- Preserve original outputs.
- Create stable outputs.
- Optionally append classifier annotations after analysis.
- Write metadata, manifest, status, logs, and exports.
- dry_run validates everything and prints the exact Rscript command without executing.
- keep_going applies if pair-level processes are run independently.
- Return a summary dictionary.

2. scripts/ytab_treated_vs_parent_screen.R

Inspect this script before editing.

Patch only when required to support:

- configurable comparison design
- configurable raw SummaryTable input paths
- configurable output directory
- configurable analysis ID
- direct Rscript execution
- stable error codes
- clear error messages
- optional parameters already present in the scientific logic

The script should accept project-resolved raw feature-table paths rather than discovering MidLC-normalized files.

Requirements:

- Preserve CPM normalization.
- Preserve scientific calculations.
- Preserve pseudocount handling.
- Preserve statistical tests.
- Preserve multiple-testing correction.
- Preserve default thresholds.
- Preserve output column names.
- Do not add MidLC normalization.
- Do not read sample_normalization directories.
- Do not read summary_normalized directories.
- Do not read combined parent tables.
- Do not read classifier outputs as analysis inputs.
- Classifier annotation, when requested, should occur after analysis, preferably in the Python wrapper.
- Avoid adding an R dependency solely for argument parsing when base R commandArgs() is sufficient.
- Report missing R packages clearly.
- Ensure:

    Rscript scripts/ytab_treated_vs_parent_screen.R --help

  works.

3. scripts/local/ytab_init_comparison_design.py

Usage:

  python scripts/local/ytab_init_comparison_design.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml

Options:

  --project-config required
  --overwrite optional
  --print-design optional

Behavior:

- Generate comparison_design.csv.
- Print inferred pairings.
- Print validation issues.
- Exit nonzero if included pairings remain ambiguous or invalid.
- Do not run the analysis.

4. scripts/local/ytab_run_treated_vs_parent.py

Usage:

  python scripts/local/ytab_run_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --analysis-id H2O2_vs_parent \
    --input-mode auto

Options:

  --project-config required
  --analysis-id optional, default treated_vs_parent_raw_cpm
  --comparison-design optional
  --comparisons optional comma-separated comparison IDs or pair IDs
  --input-mode optional, choices auto and raw-summary, default auto
  --annotate-classifier optional
  --classifier-target optional
  --force optional
  --dry-run optional
  --keep-going optional
  --extra-r-arg optional and repeatable when required

Do not add:

  --normalization-target

Behavior:

- Print project ID.
- Print species.
- Print analysis ID.
- Print comparison-design file.
- Print included pairs.
- Print requested input mode.
- Print resolved input mode:
    raw-summary
- Print raw SummaryTable input files.
- Print CPM normalization status detected from the R script.
- Print whether classifier annotation is requested.
- Print output directory.
- In dry-run mode, validate and print the exact Rscript command.
- In normal mode, run the analysis.
- Print stable result table and row count.
- Print actual scientific result columns.
- Print significance, depletion, or enrichment counts only when corresponding columns exist.
- Write metadata, manifest, and status.
- Exit nonzero on:
    invalid pairings
    missing raw SummaryTable inputs
    unconfirmed CPM normalization
    missing R packages
    analysis failure

5. scripts/local/ytab_smoke_treated_vs_parent.py

Usage:

  python scripts/local/ytab_smoke_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --comparison-id <VALID_COMPARISON_ID> \
    --input-mode auto

Behavior:

- Run one valid parent-treated pair.
- Resolve the comparison ID from comparison_design.csv.
- Do not assume a fixed sample name in general code.
- Print parent and treated sample.
- Print raw SummaryTable inputs.
- Print CPM normalization detection.
- Print output directory.
- Print stable result table.
- Print result row count.
- Print manifest.
- Print PASS/FAIL.
- Do not annotate with classifier predictions during the first smoke test.
- Do not generate a final report.

6. Update app/shiny/app.R

Add a section:

  Treated vs Parent Fitness

Place it after Essentiality Classifier.

Controls:

- project_config path
- button:
    Generate / refresh comparison design
- comparison-design preview table
- comparison-design validation issues table
- included-comparison selector
- analysis ID input
- fixed/read-only scientific input mode display:
    Raw per-sample SummaryTable
- explanatory text:
    CPM normalization is performed inside the treated-versus-parent R analysis.
- optional checkbox:
    Annotate results with classifier predictions
- classifier target selector shown only when annotation is enabled
- dry-run checkbox
- force checkbox
- keep-going checkbox
- button:
    Run treated-versus-parent analysis

Do not include controls for:

- MidLC targets
- normalized-summary inputs
- normalized-hit inputs
- combined parent inputs
- classifier target as a normalization choice

The app should:

- call ytab_init_comparison_design.py
- allow users to inspect the pairing design
- display unresolved design issues
- call ytab_run_treated_vs_parent.py
- display command and log output
- display treated_vs_parent_status.csv
- display treated_vs_parent_comparison_summary.csv when present
- display treated_vs_parent_results.csv using DT
- detect columns before creating filters or plots
- preserve scientific column names

Optional plots may be created only when appropriate columns are detected:

- fold-change distribution
- adjusted-P-value distribution
- volcano plot
- parent CPM versus treated CPM scatterplot
- replicate or pair correlation

Do not silently transform columns.

Show these warnings:

  "Treated-versus-parent analysis uses raw per-sample SummaryTable outputs and performs CPM normalization inside the R analysis."

  "MidLC normalization targets used by the essentiality classifier are not used for treated-versus-parent analysis."

  "Results from the 10,000-read smoke subset validate software behavior and should not be interpreted as a final H2O2 fitness screen."

7. Update docs/setup_local.md

Add Step 11:

  Run treated-versus-parent fitness analysis

Explain clearly:

- This workflow uses raw Step 4 SummaryTable outputs.
- CPM normalization occurs inside scripts/ytab_treated_vs_parent_screen.R.
- MidLC normalization is classifier-specific and is not used here.
- Classifier predictions are optional annotations only.
- Pairings are stored in comparison_design.csv.
- Pairings should be reviewed before analysis.
- Smoke-subset results validate software behavior only.

Commands:

Activate:

  mamba activate ytab-local

Generate and inspect comparison design:

  python scripts/local/ytab_init_comparison_design.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --print-design

Dry-run:

  python scripts/local/ytab_run_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --analysis-id H2O2_vs_parent \
    --input-mode auto \
    --dry-run

One-pair smoke:

  python scripts/local/ytab_smoke_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --comparison-id <VALID_COMPARISON_ID> \
    --input-mode auto

Full smoke-project analysis:

  python scripts/local/ytab_run_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --analysis-id H2O2_vs_parent \
    --input-mode auto \
    --keep-going

Optional classifier annotation:

  python scripts/local/ytab_run_treated_vs_parent.py \
    --project-config output/projects/<PROJECT_ID>/config/project.yaml \
    --analysis-id <ANALYSIS_ID> \
    --input-mode raw-summary \
    --annotate-classifier \
    --classifier-target <TARGET_TAG>

Do not document or demonstrate MidLC targets for Step 11.

8. VALIDATION CHECKS

Run:

  python -m py_compile src/ytab/pipeline/treated_vs_parent_runner.py
  python -m py_compile scripts/local/ytab_init_comparison_design.py
  python -m py_compile scripts/local/ytab_run_treated_vs_parent.py
  python -m py_compile scripts/local/ytab_smoke_treated_vs_parent.py

R syntax:

  Rscript -e "parse(file='scripts/ytab_treated_vs_parent_screen.R')" >/dev/null

CLI help:

  python scripts/local/ytab_init_comparison_design.py --help
  python scripts/local/ytab_run_treated_vs_parent.py --help
  python scripts/local/ytab_smoke_treated_vs_parent.py --help
  Rscript scripts/ytab_treated_vs_parent_screen.R --help

Generate design:

  python scripts/local/ytab_init_comparison_design.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --overwrite \
    --print-design

Expected design:

- four valid included parent-treated pairs
- matching yH298/yH299 backgrounds
- matching pools 1 through 4
- no unresolved included pairings

Verify input resolution:

- auto resolves to raw-summary
- each pair resolves two raw Step 4 SummaryTable feature tables
- no files are resolved from:
    sample_normalization
    summary_normalized
    combined_hits
    summary_combined
    classifier

Verify CPM:

- CPM normalization is detected in the R script
- detection evidence is recorded
- analysis stops if CPM cannot be confirmed

Dry-run:

  python scripts/local/ytab_run_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --analysis-id H2O2_vs_parent \
    --input-mode auto \
    --dry-run

One-pair smoke:

  python scripts/local/ytab_smoke_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --comparison-id <FIRST_VALID_COMPARISON_ID> \
    --input-mode auto

If the one-pair smoke passes, run all included pairs:

  python scripts/local/ytab_run_treated_vs_parent.py \
    --project-config output/projects/local_glabrata_smoke_v1/config/project.yaml \
    --analysis-id H2O2_vs_parent \
    --input-mode auto \
    --keep-going

Cache test:

- rerun the successful full command
- confirm it skips only when:
    design
    inputs
    R script
    parameters
  remain unchanged

Do not enable classifier annotation during initial validation.

Expected outputs:

  output/projects/local_glabrata_smoke_v1/config/comparison_design.csv

  output/projects/local_glabrata_smoke_v1/treated_vs_parent/H2O2_vs_parent/

  output/projects/local_glabrata_smoke_v1/treated_vs_parent/H2O2_vs_parent/treated_vs_parent_results.csv

  output/projects/local_glabrata_smoke_v1/treated_vs_parent/H2O2_vs_parent/treated_vs_parent_comparison_summary.csv

  output/projects/local_glabrata_smoke_v1/treated_vs_parent/H2O2_vs_parent/treated_vs_parent_run_metadata.json

  output/projects/local_glabrata_smoke_v1/manifests/treated_vs_parent/H2O2_vs_parent.treated_vs_parent_manifest.json

  output/projects/local_glabrata_smoke_v1/manifests/treated_vs_parent/treated_vs_parent_status.csv

  output/exports/local_glabrata_smoke_v1/treated_vs_parent/H2O2_vs_parent/

9. HARD-CODED PATH CLEANUP

Run:

  grep -R "/Users/jayelazuno\\|/old_Users\\|/Volumes/rdss\\|/mnt/archived_data\\|/workspace/HermesTnseq" -n \
    src/ytab/pipeline/treated_vs_parent_runner.py \
    scripts/local/ytab_init_comparison_design.py \
    scripts/local/ytab_run_treated_vs_parent.py \
    scripts/local/ytab_smoke_treated_vs_parent.py \
    scripts/ytab_treated_vs_parent_screen.R \
    app/shiny/app.R \
    docs/setup_local.md

Rules:

- Do not introduce machine-specific paths.
- Existing legacy HPC files outside the modified Step 11 scope may retain old paths.
- The local Step 11 workflow must use project-relative paths.

10. APP ORGANIZATION COMPATIBILITY

Ensure this workflow:

  Shiny app
    -> project.yaml
    -> sample_sheet.csv
    -> comparison_design.csv
    -> raw summary/<sample>/ feature tables
    -> ytab_run_treated_vs_parent.py
    -> scripts/ytab_treated_vs_parent_screen.R
    -> CPM normalization inside R
    -> treated_vs_parent/<ANALYSIS_ID>/
    -> manifest/status/metadata
    -> Shiny displays results

Users must not need to edit Python or R source code.

11. STOP POINT

Stop after Step 11.

Do not implement:

- final PDF or HTML report generation
- one-click complete-pipeline orchestration
- publication-ready final figure assembly
- automated biological interpretation
- deployment changes
- package installation redesign
- new statistical models
- additional normalization methods

After implementation, show:

  git status --short

  find \
    src/ytab/pipeline \
    scripts/local \
    app/shiny \
    output/projects/local_glabrata_smoke_v1/treated_vs_parent \
    output/projects/local_glabrata_smoke_v1/manifests/treated_vs_parent \
    output/exports/local_glabrata_smoke_v1/treated_vs_parent \
    -maxdepth 5 -type f | sort

Summarize:

- files created and modified
- whether comparison-design generation passed
- inferred parent-treated pairings
- whether design validation passed
- confirmation that auto resolved to raw-summary
- raw SummaryTable files used
- confirmation that no MidLC-normalized files were used
- whether CPM normalization was confirmed in the R script
- CPM detection evidence
- scientific analysis script used
- normalization and statistical methods detected
- whether dry-run passed
- whether one-pair smoke passed
- whether full four-pair analysis passed
- number of result rows
- actual scientific output columns
- depletion or enrichment counts only when emitted by the R analysis
- stable result-table path
- whether cache-aware rerun skipped correctly
- whether classifier annotation was used
- warnings or missing resources
- confirmation that final report generation did not run
