#!/usr/bin/env Rscript
args<-commandArgs(trailingOnly=TRUE);stopifnot(length(args)==1L);all<-commandArgs(trailingOnly=FALSE);self<-normalizePath(sub("^--file=","",grep("^--file=",all,value=TRUE)[[1]]),winslash="/");root<-normalizePath(file.path(dirname(self),"../.."),winslash="/");library(shiny);`%||%`<-function(l,r)if(is.null(l)||!length(l))r else l
for(file in c("project_discovery.R","preprocessing_status.R","ui_qc.R"))source(file.path(root,"app/shiny/R",file),local=TRUE)
info<-read_project_summary(args[[1]],root);status<-build_sample_pipeline_status(info);eligible<-qc_sample_eligibility(info,status);all_names<-eligible$Sample[eligible$Eligible=="Yes"];parents<-eligible$Sample[eligible$Eligible=="Yes"&tolower(eligible$Condition)=="parent"];treated<-eligible$Sample[eligible$Eligible=="Yes"&tolower(eligible$Condition)=="treated"]
stopifnot(length(all_names)==8L,length(parents)==4L,length(treated)==4L,all(vapply(status$hits_path[match(all_names,status$sample)],nonempty_file,FALSE)));state<-reactiveVal(all_names);state(parents);stopifnot(setequal(isolate(state()),parents));state(treated);stopifnot(setequal(isolate(state()),treated));state(character());stopifnot(!length(isolate(state())));state(all_names[1:2]);stopifnot(identical(isolate(state()),all_names[1:2]));preprocessing<-rev(all_names);stopifnot(identical(isolate(state()),all_names[1:2]));cli<-c("--samples",paste(isolate(state()),collapse=","));stopifnot(identical(strsplit(cli[[2]],",",fixed=TRUE)[[1]],isolate(state())));ui_text<-paste(deparse(body(qc_library_diagnostics_ui)),collapse=" ");stopifnot(grepl("selectizeInput",ui_text),!grepl("textInput.*samples",ui_text));cat("PASS\n")
